.container
  .row
    .col-sm-12
      %h1
        = @items.count
        Items
  .row
  - @items.each do |item|
    .col-sm-3
      %h5= link_to(item.name, item_path(item))
      -# = link_to(image_tag(item.image_url), item_path(item))
      %p
        = item.description
